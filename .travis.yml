language: c
sudo: required
compiler: gcc
dist: trusty
before-script:
  - sudo apt-get install g++
  - sudo apt-get install yasm
  - git clone https://github.com/vertis/objconv
  - g++ -o /usr/bin/objconv -O2 objconv/src/*.cpp
script: make
